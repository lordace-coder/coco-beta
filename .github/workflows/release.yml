name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.asset_name }}
    runs-on: ${{ matrix.runner }}

    strategy:
      matrix:
        include:
          - runner: ubuntu-latest
            goos: linux
            goarch: amd64
            asset_name: cocobase-linux-amd64
            cc: musl-gcc
            static: true

          - runner: ubuntu-latest
            goos: linux
            goarch: arm64
            asset_name: cocobase-linux-arm64
            cc: aarch64-linux-gnu-gcc
            static: false

          - runner: macos-latest
            goos: darwin
            goarch: amd64
            asset_name: cocobase-macos-amd64
            cc: clang

          - runner: macos-latest
            goos: darwin
            goarch: arm64
            asset_name: cocobase-macos-arm64
            cc: clang

          - runner: windows-latest
            goos: windows
            goarch: amd64
            asset_name: cocobase-windows-amd64.exe
            cc: gcc

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22.x"   # stable release that supports all runners

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Linux build deps
        if: matrix.goos == 'linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y musl-tools gcc-aarch64-linux-gnu

      - name: Build dashboard
        shell: bash
        working-directory: dashboard
        run: |
          npm ci
          npm run build

      - name: Build binary (Unix)
        if: matrix.goos != 'windows'
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "1"
          CC: ${{ matrix.cc }}
        run: |
          LDFLAGS="-s -w -X main.Version=${{ github.ref_name }}"
          if [ "${{ matrix.static }}" = "true" ]; then
            LDFLAGS="$LDFLAGS -extldflags '-static'"
          fi
          go build \
            -ldflags="$LDFLAGS" \
            -o dist/${{ matrix.asset_name }} \
            ./cmd/cocobase
          ls -lh dist/

      - name: Build binary (Windows)
        if: matrix.goos == 'windows'
        shell: cmd
        env:
          GOOS: windows
          GOARCH: amd64
          CGO_ENABLED: "1"
        run: |
          mkdir dist
          go build -ldflags="-s -w -X main.Version=${{ github.ref_name }}" -o dist\${{ matrix.asset_name }} .\cmd\cocobase
          dir dist\

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: dist/${{ matrix.asset_name }}
          generate_release_notes: true
          fail_on_unmatched_files: true
